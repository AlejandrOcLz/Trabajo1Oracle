CREATE TABLE Usere (
    ids VARCHAR(255) PRIMARY KEY,
    update_at DATE,
    created_at DATE,
    email VARCHAR(255),
    passwords VARCHAR(255),
    first_name VARCHAR(255),
    last_name VARCHAR(255)
);